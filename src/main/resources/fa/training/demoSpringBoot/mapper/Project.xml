<?xml version = "1.0" encoding = "UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="fa.training.demoSpringBoot.mapper.ProjectMapper">
    <!--<insert id="insertStudent" parameterType="Student" >
        INSERT INTO STUDENT (ID, NAME, BRANCH, PERCENTAGE, PHONE, EMAIL ) VALUES (#{id}, #{name}, #{branch}, #{percentage}, #{phone}, #{email});
    </insert>
 
    <update id="updateStudent" parameterType="Student">
        UPDATE STUDENT SET EMAIL = #{email}, NAME = #{name}, BRANCH = #{branch}, PERCENTAGE = #{percentage}, PHONE = #{phone} WHERE ID = #{id};
    </update>
     
    <delete id="deleteStudentById" parameterType="int">
        DELETE from STUDENT WHERE ID = #{id};
    </delete>-->
     
    <select id="selectAllProject" resultMap="result">
        SELECT * FROM projects;
    </select>
     
    <select id="selectProjectById" parameterType="int" resultType="Project">
        SELECT * FROM projects WHERE project_id = #{project_id};
    </select>
     
    <resultMap id = "result" type = "Project">
       <result property = "project_id" column = "project_id"/>
       <result property = "project_name" column = "project_name"/>
       <result property = "dept_id" column = "dept_id"/>
    </resultMap>
</mapper>